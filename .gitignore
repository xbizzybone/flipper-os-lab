# flipper-os-lab — git ignores

# Runtime artifacts (loop images, mounts, verity hashes)
run/
*.squashfs
*.verity
*.roothash
*.btrfs
*.ext4
tampered.img

# Host-specific scratch runners & throwaway logs (not part of the PoC)
run_wsl_lab.sh
*.vbs
*.bat
*.log
*.log.gz
docker_ps*.txt
wsl_list.txt
wsl_lab.log

# Stray nested test repo, never commit
test_git_tmp/

# Internal collaboration notes (private handoff, not for the public repo)
COLLABORATION-STATUS.md
